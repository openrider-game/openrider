export const PI2 = 2 * Math.PI,
    rand = Math.random,
    cos = Math.cos,
    sin = Math.sin,
    round = Math.round,
    ceil = Math.ceil,
    floor = Math.floor,
    pow = Math.pow,
    sqrt = Math.sqrt,
    toInt = parseInt;